#include <iostream>

using namespace std;

int main() {
    const char* message = "Hello";

    // Loop through each character until null terminator
    cout << message.size() << endl;

    return 0;
}
